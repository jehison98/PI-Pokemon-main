const { getDataPokemons } = require("./getDataPokemons");
const { getPokemonsType } = require("./getPokemonsType");
const { getTwoTypes } = require("./getTwoTypes");
const { orderPokemons } = require("./orderPokemons");

module.exports = {
  getDataPokemons,
  getPokemonsType,
  getTwoTypes,
  orderPokemons,
};
